import 'package:flutter/material.dart';

class RoutesTransition extends StatelessWidget {
  const RoutesTransition({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
  
  }